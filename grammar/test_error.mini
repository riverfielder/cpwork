program example;
begin
    x := 10
    y := 20;  // 缺少分号
    
    if x > y then
        max := x
    else       // then分支缺少分号
        max := y;
    
    while x < 100  // 缺少do关键字
    begin
        x := x + 1
        sum := sum + x;
    end;
    
    is_valid := true
end
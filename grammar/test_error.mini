program error_example;
begin
    // 缺少分号
    x := 10
    y := 20;
    
    // if缺少then
    if (x > y)
        max := x;
    
    // while缺少do
    while (i < 10)
        i := i + 1;
    
    // 不完整表达式
    z := 1 + ;
    
    // 未闭合的块
    begin
        a := 5
end.